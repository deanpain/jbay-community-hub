// STUB — replace with valid Midnight Compact.
// Module: listings (private MLS entries scoped by municipality config).
//
// Bulletin Board Template Mapping:
// - Board ID -> Category Tab (Education, Recreation, Entertainment)
// - Post ID -> Listing.id
// - Post Payload -> { organisation, title, summary, schedule, proof_requirements }
// - Poster Identity -> Shielded (verified-resident credential check)
//
// Responsibilities:
// - Create/update/list shielded listing payloads per category (education | recreation | entertainment).
// - Map stable listing identifiers (see `@jbay/shared` Listing.id) to shielded payloads — Bulletin Board pattern from Midnight templates.
// - Enforce verified-resident gate via ZK credential checks (see verified_resident.compact.stub).
// Dependencies: Bulletin Board–style commitment graph once ported from templates/.
